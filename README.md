
  # Create functional prototype

  This is a code bundle for Create functional prototype. The original project is available at https://www.figma.com/design/HhsgUcU3QOD8uSPdT8nsjQ/Create-functional-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  